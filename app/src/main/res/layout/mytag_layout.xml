<?xml version="1.0" encoding="utf-8"?>
<!--我的话题-->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="32dp"
    android:paddingTop="@dimen/px_sixteen"
    android:paddingBottom="@dimen/px_sixteen"
    android:paddingLeft="14dp"
    android:paddingRight="14dp"
    android:background="@drawable/mytag_item_bg"
    android:orientation="vertical">

    <TextView
        android:id="@+id/bt_tag"
        android:layout_width="wrap_content"
        android:layout_centerVertical="true"
        android:textColor="@color/ali_feedback_color_white"
        android:layout_height="wrap_content"
        android:background="@color/transparent"
        android:text="标签1"
        android:maxWidth="@dimen/px_tag_max_width"
        android:singleLine="true"
        android:ellipsize="end"
        android:textSize="16sp" />
</RelativeLayout>